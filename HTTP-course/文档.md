```
HTTP
    ins传播图片的例子

    protocol  协议
        定义了一系列沟通的规则
        0101100
        parse 解析

    request/response
        context 上下文
        life-cycle
        Clients/Servers
            FE
                interact view
            BE
                data center
    URL

    Fetch API
        await promise
        异步
        json
            JavaScript对象
            key value pairs
    
    Web Addresses
        IP addresses  unique
        IPv4
            8.13.156.7 0~255
        Ipv6
            :
        DNS
            domain --> IP
            mapping
            1. Resolve DNS
            2. Use IP

            ICANN
                root nameserver
                subdomin
            
        
        API
            JSONLint
        部署服务器
            域名
            
            URL API

    URI 
        Uniform Resource Identifier 
        统一资源标识符  URL的超集
        URN
        
    port path query?key-pairs # achor 

    解析URL
        HTTP 
           80
        HTTP 
           443
    
    ports
        https://caddyserver.com/
    
    synchronous code
    Asynchronous processing
        callback

        进程间的同步和异步
 
    IO事件
        磁盘驱动器读取文件
        蓝牙设备交互的HTTP的请求
    
    I/O Timings
        RAM      ns 纳秒         syhc         读写变量
        disk     1s              asyhc/sync   加载大型文件
        Network  100ms-2000ms    asyhc        网络请求

        阻塞

    async/await  promise
    try/catch

    Bugs
        bad unexpected
        debugger
            逐步调式
    Errors
        not always bad
        expected
        no internet connection
        Remote Server Pown
        User input
    
    HTTP header
        身份验证
        additional metadata 元数据

    Browser's Developer

    JSON
        plain text
        key value pairs  / arrays
 
```